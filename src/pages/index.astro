---
import { getCollection } from "astro:content";
import PostCard from "../components/post/PostCard.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { SITE_AUTHOR_PROFILE } from "../lib/site";

const [topics, posts, concepts] = await Promise.all([
  getCollection("topics"),
  getCollection("posts", ({ data }) => !data.draft),
  getCollection("concepts")
]);

const sortedTopics = topics.sort((a, b) => {
  const orderA = a.data.order ?? Number.POSITIVE_INFINITY;
  const orderB = b.data.order ?? Number.POSITIVE_INFINITY;
  if (orderA !== orderB) {
    return orderA - orderB;
  }
  return a.data.title.localeCompare(b.data.title, "zh-CN");
});

const latestPosts = [...posts]
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
  .slice(0, 6);

const postCountByTopic = posts.reduce<Record<string, number>>((acc, post) => {
  post.data.topics.forEach((topicSlug) => {
    acc[topicSlug] = (acc[topicSlug] ?? 0) + 1;
  });
  return acc;
}, {});

const heroLinks = SITE_AUTHOR_PROFILE.socials?.github
  ? [
      {
        href: `https://github.com/${SITE_AUTHOR_PROFILE.socials.github}`,
        label: "GitHub",
        external: true
      },
      { href: "/author", label: "作者页" },
      { href: "/archives", label: "阅读档案" }
    ]
  : [
      { href: "/author", label: "作者页" },
      { href: "/archives", label: "阅读档案" }
    ];

const heroNotes = [
  {
    title: "Topic-first",
    body: "用主题而不是时间流组织知识，减少读者的入口噪声。"
  },
  {
    title: "Paragraph-aware",
    body: "评论贴附到段落边缘，讨论与上下文保持稳定关联。"
  },
  {
    title: "Research-ready",
    body: "适合长文、代码、脚注、参考文献和持续迭代的技术写作。"
  }
];
---

<BaseLayout title="MyBlog | 主题化知识网络">
  <section class="home-hero content-section" data-ux-reveal="fade-up" aria-label="博客简介">
    <div class="home-hero-copy">
      <p class="home-hero-kicker">A-Znk · AI / 技术研究博客</p>
      <h1 class="home-hero-title">主题先行的研究型个人博客</h1>
      <p class="home-hero-sub">
        从主题地图进入，用文章展开判断，在段落边缘留下批注。
        这里记录 AI、技术和知识组织方式，而不是一条只会被时间冲走的 feed。
      </p>
      <div class="home-hero-actions">
        <a href="/topics" class="home-hero-btn home-hero-btn--primary">浏览主题</a>
        <a href="/archives" class="home-hero-btn home-hero-btn--ghost">进入归档</a>
      </div>
      <div class="home-hero-links" aria-label="站点快捷入口">
        {heroLinks.map((link) => (
          <a
            href={link.href}
            class="home-hero-link"
            target={link.external ? "_blank" : undefined}
            rel={link.external ? "noreferrer noopener" : undefined}
          >
            {link.label}
          </a>
        ))}
      </div>
    </div>
    <div class="home-hero-panel" aria-label="博客能力概览">
      <div class="home-hero-panel-head">
        <p class="home-hero-panel-kicker">Research Modes</p>
        <p class="home-hero-panel-title">Refined, technical, calm.</p>
      </div>
      <ul class="home-hero-note-list">
        {heroNotes.map((note) => (
          <li class="home-hero-note">
            <strong>{note.title}</strong>
            <span>{note.body}</span>
          </li>
        ))}
      </ul>
      <div class="home-hero-stats" aria-label="博客统计">
        <div class="home-hero-stat">
          <span class="home-hero-stat-value">{posts.length}</span>
          <span class="home-hero-stat-label">篇文章</span>
        </div>
        <div class="home-hero-stat">
          <span class="home-hero-stat-value">{topics.length}</span>
          <span class="home-hero-stat-label">个主题</span>
        </div>
        <div class="home-hero-stat">
          <span class="home-hero-stat-value">{concepts.length}</span>
          <span class="home-hero-stat-label">个概念</span>
        </div>
      </div>
    </div>
  </section>

  <section class="grid topic-grid content-section" data-ux-stagger>
    {
      sortedTopics.map((topic) => (
        <article class="card" data-ux-reveal="fade-up">
          <h3>
            <a href={`/topics/${topic.slug}`}>{topic.data.title}</a>
          </h3>
          <p>{topic.data.summary}</p>
          <p class="subtle" style="margin-top: 0.6rem;">
            相关文章 {postCountByTopic[topic.slug] ?? 0} 篇
          </p>
        </article>
      ))
    }
  </section>

  <section class="content-section" data-latest-posts data-ux-reveal="fade-up">
    <h2>最新文章</h2>
    <p class="subtle">每篇文章都提供封面图，帮助你更快定位主题和上下文。</p>

    {
      latestPosts.length === 0 ? (
        <p class="subtle">暂时还没有可展示的文章。</p>
      ) : (
        <div class="grid post-card-grid" data-ux-stagger>
          {latestPosts.map((post) => <PostCard post={post} />)}
        </div>
      )
    }
  </section>

  <section class="content-section home-archive-entry" data-home-archive-entry data-ux-reveal="fade-up">
    <h2>时间归档</h2>
    <p class="subtle">按年与月份浏览全部文章，快速定位你错过的内容与写作节奏。</p>
    <a class="archive-entry-button" href="/archives">进入归档时间轴</a>
  </section>
</BaseLayout>
